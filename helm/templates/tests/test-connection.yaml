apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "new-helm.fullname" . }}-test-connection"
  labels:
    {{- include "new-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "new-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
